apiVersion: v1
kind: Service
metadata:
  name: {{ include "rebm-slack-bot.fullname" . }}
  labels:
    {{- include "rebm-slack-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rebm-slack-bot.selectorLabels" . | nindent 4 }} 