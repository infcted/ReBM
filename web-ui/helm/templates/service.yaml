apiVersion: v1
kind: Service
metadata:
  name: {{ include "rebm-web-ui.fullname" . }}
  labels:
    {{- include "rebm-web-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rebm-web-ui.selectorLabels" . | nindent 4 }} 